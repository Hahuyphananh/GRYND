import PageClient from "./PageClient";

export const metadata = {
  title: "Accessibility — GoonBet",
  description:
    "GoonBet is committed to accessibility — keyboard navigation, screen-reader support, contrast, focus indicators and reduced motion.",
};

export default function Page() {
  return <PageClient />;
}
