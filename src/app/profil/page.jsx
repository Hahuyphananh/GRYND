import PageClient from "./PageClient";

export const metadata = {
  title: "My Profile — GoonBet",
  description:
    "Manage your GoonBet profile — track your balance, XP, level, titles, streaks and game statistics.",
};

export default function Page() {
  return <PageClient />;
}
