import PageClient from "./PageClient";

export const metadata = {
  title: "Sign Up — GoonBet",
  description:
    "Create your free GoonBet account and claim free virtual tokens. No real money needed — play skill-based games, win rewards and climb the leaderboard.",
};

export default function Page() {
  return <PageClient />;
}
